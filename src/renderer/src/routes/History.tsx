export default function History() {
  return (
    <div className="p-10">
      <h1 className="text-3xl text-ink/90">History</h1>
      <p className="mt-2 max-w-xl text-muted">
        The spiral library. Past thoughts and rewrites, searchable — so the next time the same
        thought hits, you can recall what worked instead of writing a new rewrite from scratch.
      </p>
      <p className="mt-8 text-xs uppercase tracking-wider text-muted/60">
        coming next: list view + search
      </p>
    </div>
  )
}
